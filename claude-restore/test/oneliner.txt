if((Get-ItemProperty 'HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager' -Name PendingFileRenameOperations -EA 0).PendingFileRenameOperations){'REBOOT FIRST, then paste this again.'}else{Get-Process claude -EA 0|Stop-Process -Force -EA 0;$b="$env:USERPROFILE\Desktop\Claude-backup-$(Get-Date -f yyyyMMdd-HHmmss)";if(Test-Path "$env:APPDATA\Claude"){New-Item -ItemType Directory $b -Force|Out-Null;Copy-Item "$env:APPDATA\Claude" $b -Recurse -Force -EA 0;"Settings backed up to $b"};Get-AppxPackage *Claude*|Remove-AppxPackage -EA 0;Remove-Item "$env:LOCALAPPDATA\Packages\*Claude*","$env:LOCALAPPDATA\AnthropicClaude","$env:APPDATA\Claude" -Recurse -Force -EA 0;if(Get-Item "$env:LOCALAPPDATA\Packages\*Claude*","$env:LOCALAPPDATA\AnthropicClaude" -EA 0){'INCOMPLETE - reboot, then paste again.'}else{'PASS. Install now.';Start-Process 'https://claude.ai/download'}}
